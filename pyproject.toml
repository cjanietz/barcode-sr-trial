[project]
name = "sr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "pillow",
    "python-barcode[images]",
    "qrcode[pil]",
    "opencv-python",
    "pylibdmtx",
]
